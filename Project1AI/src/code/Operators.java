package code;

public enum Operators {
	 REQUEST_FOOD, REQUEST_MATERIALS, REQUEST_ENERGY, WAIT, BUILD1, BUILD2; 
	
	public String toString() {
        return name();  // Or any other representation that makes sense for your application
    }
}
